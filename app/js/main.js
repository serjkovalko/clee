$(function () {

  $('.top-slider__inner').slick({
     dots: true,
     arrows: false,
     fade: true,
     autoplay: true,
     autoplaySpeed: 3000
  });
  var mixer = mixitup('.categories-products');
  var mixer = mixitup('.design__items');
});